#ifndef JsonReader_H
#define JsonReader_H

#include <fstream>
#include <nlohmann/json.hpp>

class JsonReader
{
    public:
    //Initialise the Class
    JsonReader();

    //Destroy the Class
    ~JsonReader();
    private:
};

#endif